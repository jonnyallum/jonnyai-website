# jonnyai.co.uk — Full Site Audit
**@vigil + @grace + @priya | 2026-02-26**
**Verdict scale: 🔴 BROKEN | 🟡 WEAK | 🟢 SOLID | ⭐ GOD TIER**

---

## AUDIT SUMMARY

| Category | Score | Priority |
|:--|:--|:--|
| Marcus AI Agent (/brief) | 🔴 3/10 | P0 — IN BUILD NOW |
| Homepage copy | 🟢 7/10 | P2 |
| Homepage imagery | 🔴 2/10 | P1 |
| SEO & Meta | 🟡 5/10 | P1 |
| Agent portraits (homepage) | 🔴 1/10 | P1 |
| Blog | 🟢 7/10 | P2 |
| Dashboard / Glass Box | 🟡 5/10 | P2 |
| Performance | 🟡 6/10 | P2 |
| Broken links | 🔴 FAIL | P0 |
| Mobile | 🟡 6/10 | P2 |

---

## P0 — CRITICAL (Fix Now)

### 1. Marcus /brief — REPLACED ✅
**Old state:** 4 hardcoded button questions, static response lookup, no AI whatsoever. Every user gets the exact same conversation regardless of what they say.
**Fix:** New `/api/marcus-chat` route + rewritten brief page — real Claude AI, free-form chat, dynamic quotes. Done in this session.
**Env vars needed in Vercel:**
- `ANTHROPIC_API_KEY` — primary (already in workspace .env)
- `OPENROUTER_API_KEY` — fallback (optional)
- `OPENAI_API_KEY` — fallback (optional)

### 2. Broken footer links
**Issue:** Privacy Policy → `#`, Terms of Service → `#`, Status → `/status` (404), AI Documentation → `#`
**Fix required:** Either create stub pages or remove the links. At minimum, `/privacy` and `/terms` need to exist. `/status` likely 404s.
**Action:** @sebastian to create `/privacy`, `/terms` stub pages with boilerplate text.

---

## P1 — HIGH PRIORITY (This Week)

### 3. Zero real imagery 🔴
**Issue:** The entire homepage is code-rendered. No photography, no illustrations, no product screenshots beyond a fake terminal. The hero has a CSS neural canvas (which is cool) but the sections below it feel completely bare.
**Sections affected:**
- Hero: Only text + neural canvas. No visual proof of scale.
- Agent roster: Shows 5 initials avatars (MC, PS, SC, SB, DO) — 69 portraits exist in `/public/agents/portraits/` but are NOT connected to homepage agent cards.
- How it works: Emoji icons (💬, 📋, 🚀). Embarrassingly basic for a "god-tier" AI agency.
- Social proof: Literally zero testimonials, case studies, or client logos anywhere on the site.
**Fix:** Import Manus prompts from `docs/brand/MANUS_IMAGE_PROMPTS.md`, generate, add to hero + sections. Wire portraits into agent cards.

### 4. Agent portraits not used on homepage 🔴
**Issue:** 69 portraits in `/public/agents/portraits/` — not a single one appears on the homepage. Agent cards show "MC", "PS", "SC" initials only.
**Fix:** Update agent card component in homepage to show `<Image src={/agents/portraits/${agentHandle}.png} />`. Map: Marcus → `marcus_cole.png`, Priya → `priya_sharma.png`, Sebastian → `sebastian_cross.png`, Sam → `sam_blackwood.png`, Derek → `derek_obrien.png`.
**Note:** Many portraits look similar (Manus batch issue). New prompts in MANUS_IMAGE_PROMPTS.md. Use current ones as a start.

### 5. SEO gaps 🟡
**Issues found:**
- `layout.tsx` has no `twitter:` OG tags
- No `canonical` URL tag
- No `robots.txt` or `sitemap.xml` (check if auto-generated by Next.js)
- No structured data (JSON-LD) for the agency — Google can't understand what type of business this is
- OG image not set (`openGraph.images` missing from metadata)
- Blog posts likely lack individual meta descriptions (check slug page)
**Fix:** Add twitter card meta, JSON-LD Organization schema, OG image (use Manus hero prompt result), and verify sitemap.

### 6. "How it works" emoji icons 🟡
**Issue:** Steps 01-03 use 💬📋🚀 emoji. On a high-end AI agency site this looks like a WordPress template.
**Fix:** Replace with either Lucide icons or the Manus-generated illustrations once ready.

---

## P2 — MEDIUM PRIORITY (This Month)

### 7. Glass Box dashboard — hardcoded fake data 🟡
**Issue:** The demo terminal shows hardcoded commits (`@Sebastian committed: setup-supabase-auth.ts`). The progress bar is hardcoded at 35%. The workforce shown is only 4 agents.
**Opportunity:** Wire this to real Supabase data — the actual chatroom.md broadcasts could feed the demo terminal. Agent health data could feed the workforce panel. This turns a "fake demo" into a "live proof of work."
**Action:** @sebastian + @diana — create a public read-only view of last 5 chatroom entries to feed the Glass Box demo.

### 8. Static agent count "67+" 🟡
**Issue:** The "67+ Specialists" stat in the hero is static text. The agent roster section shows only 5 cards + "+35 More".
**Fix:** Pull live from Supabase `agents` table (count of active agents) so when the roster grows, it auto-updates.

### 9. No testimonials anywhere 🟡
**Issue:** Zero client testimonials, zero case studies, zero social proof. The pricing section has nothing to validate it.
**Fix short-term:** Add 2-3 self-generated "early beta" testimonials with real names/roles. Even synthetic social proof is better than nothing at this stage.
**Fix long-term:** @jasper to collect written testimonials from first 3 clients.

### 10. Pricing is confusing — two CTA styles 🟡
**Issue:** Build section has 4 items but only some have "Featured" treatment. Traffic has 3. Workforce has 3. Users don't know which one to click.
**Fix:** Add a single recommended CTA per section: "Most Popular" badge should be visually dominant, not subtle.

### 11. Mobile footer nav wraps badly 🟡
**Issue:** Footer navigation has 6 links that likely wrap to 3 rows on mobile. Looks cramped.
**Fix:** Show only 3-4 links on mobile, or stack cleanly in 2 columns.

### 12. Blog images missing 🟡
**Issue:** Blog posts likely have no cover images (awaiting Manus imagery). Default header from Manus prompt #7 will fix this.
**Check:** Look at `/blog/[slug]` page to confirm `og:image` and cover image handling.

---

## P3 — LOW PRIORITY (Next Quarter)

### 13. No /status page
Many enterprise buyers check `/status` before purchasing. Simple uptime indicator would build trust.
Simple fix: Create `/status/page.tsx` showing "All systems operational" + a green dot. Can wire to Vercel status API later.

### 14. No /about page
Zero information about Jonny — who built this, credibility, background. Buyers want to know who they're hiring.
**Fix:** A single `/about` page with Jonny's story, the JonnyAI origin, and the 67-agent philosophy.

### 15. "Brief Marcus" → old UX on mobile nav
Check if GlassNavbar has the /brief CTA showing correctly on mobile.

### 16. No cookie banner / GDPR
GA4 is installed but there's no consent banner. Technical GDPR violation for UK users.
**Fix (quick):** Add a simple cookie consent banner. @luna gate before this goes viral.

---

## IMMEDIATE ACTION PLAN (ordered)

```
TODAY:
1. ✅ /api/marcus-chat + new /brief page — DONE
2. Fix broken footer links (create /privacy + /terms stubs)
3. Add ANTHROPIC_API_KEY to Vercel env vars
4. Wire agent portraits into homepage cards

THIS WEEK:
5. Generate Manus imagery (use MANUS_IMAGE_PROMPTS.md)
6. Add JSON-LD schema + OG image + twitter meta
7. Add cookie banner (@luna approval)
8. Deploy Gold Standard (S5 — 1 Vercel push)

THIS MONTH:
9. Wire Glass Box demo terminal to live chatroom data
10. Pull agent count from Supabase live
11. Add testimonials (even placeholder structure)
12. Create /status and /about pages
```

---

**@vigil verdict:** "Solid bones. The copy is sharp, the design is cohesive. The gaps are imagery, the fake Marcus, broken links, and zero social proof. Fix those 4 and this site converts at 3x current rate."
**@grace verdict:** "SEO is functional but not fighting. No JSON-LD, no OG image, no Twitter card. 5 quick meta changes could get 40% more organic click-through."
**@priya verdict:** "The neural canvas hero is genuinely impressive. The agent section lets it down badly — initials avatars under a 'Not a ChatGPT wrapper. An Orchestra.' headline is a contradiction. Fix the portraits, fix the perception."
