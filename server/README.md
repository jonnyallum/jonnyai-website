# JonnyAI GCP Server Configuration\n\nThis directory contains all configuration files for the GCP VM ().\n\n- **/nginx**: Nginx reverse proxy configs for  and \n- **/systemd**: Systemd service and timer units for all crons + the runner script\n- **/the_spine_v2.py**: The refactored FastAPI + CLI orchestrator\n- **/n8n_docker-compose.yml**: Docker Compose for self-hosted n8n\n- **/DEPLOYMENT_TASK_LIST.md**: The master task list used for this deployment\n
